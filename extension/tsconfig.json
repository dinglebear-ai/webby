{
  "$comment": "Type-checks the WebMCP probe against the published upstream definitions. This is a contract check, not a build step: nothing is emitted, and only the files that touch the WebMCP surface are included. See docs/upstream-contracts.md.",
  "compilerOptions": {
    "target": "ES2023",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2023", "DOM"],

    "allowJs": true,
    "checkJs": true,
    "noEmit": true,

    "strict": true,
    "noUncheckedIndexedAccess": true,

    "types": ["webmcp-types"],

    "skipLibCheck": true
  },
  "include": ["src/probe.js", "src/globals.d.ts"]
}
